# bench_loop pod identity.
# POD_ID is resolved to ip/port/key at runtime via `runpodctl ssh info $POD_ID`
# (community pods churn — never trust a hardcoded address).
# The FALLBACK_* values are used ONLY when runpodctl resolution fails, with a loud
# warning: they may be stale if the pod restarted. Refresh them when the pod moves.
POD_ID=5gw3c25kdtmtm7
FALLBACK_HOST=213.144.200.241
FALLBACK_PORT=12893
FALLBACK_KEY=/Users/theodorepender/.runpod/ssh/runpodctl-ssh-key
