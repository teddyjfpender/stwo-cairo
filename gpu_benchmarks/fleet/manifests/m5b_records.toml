# M5b records: the record steps that the m5b_validate run missed when its
# merkle-spans must_match regex crashed gpufleet. gpu-native identity already
# validated (M5B_PROOF_MATCH, A2 engaged) in the prior run.
#
#   gpufleet run manifests/m5b_records.toml --pod <ID> --skip-pregate --keep

[meta]
name = "m5b-records"
stop_on_failure = true

[env]
RUST_MIN_STACK = "4194304"
STWO_JIT_MAX_KERNEL_INSTRS = "2048"
STWO_JIT_CUBIN_CACHE = "1"
GB = "/workspace/stwo-cairo/stwo_cairo_prover/target/release/gpu_bench"
SN = "/workspace/stwo-cairo/gpu_benchmarks/pie/sn"

[[step]]
name = "m5b-sn3"
cmd = '''
$GB --pie $SN/SN_PIE_3.zip --backend cuda --engine gpu-native --reps 2 \
  --reuse-input 2>/dev/null | tail -1'''
timeout_s = 10800
gpu_bound = true

[[step]]
name = "m5b-sn4"
cmd = '''
$GB --pie $SN/SN_PIE_4.zip --backend cuda --engine gpu-native --reps 2 \
  --reuse-input 2>/dev/null | tail -1'''
timeout_s = 10800
gpu_bound = true

[[step]]
name = "m5b-sn1"
cmd = '''
$GB --pie $SN/SN_PIE_1.zip --backend cuda --engine gpu-native --reps 2 \
  --reuse-input 2>/dev/null | tail -1'''
timeout_s = 10800
gpu_bound = true

[[step]]
name = "m5b-sustained"
cmd = '''
$GB --pie $SN/SN_PIE_2.zip --backend cuda --engine gpu-native \
  --pipeline 3 --producers 3 --reuse-input 2>/dev/null | tail -3'''
timeout_s = 10800
gpu_bound = true
